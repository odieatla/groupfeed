{
  "name": "groupfeed",
  "version": "0.0.1",
  "description": "application that allows user to read Instagram feed based on group",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4"
  }
}
